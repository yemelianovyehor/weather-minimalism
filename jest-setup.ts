import "@testing-library/jest-dom";
// import * as data from "__mocks__/Weatherdata.json";
// import axios, { AxiosResponse } from "axios";
// import ApiAnswer from "src/types/ApiAnswer";

/* const mockedResp = {
	data: data,
	status: 200,
	statusText: "OK",
	headers: {},
	config: {},
}; */

// jest.mock("axios", () => {
// 	get: () => new Promise<ApiAnswer>(() => mockedResp);
// });

// jest.mock("axios")
// const mockedAxios = jest.mocked(axios)
// axios.get.mockImplementation(()=>Promise.resolve(mockedResp))
// console.log(axios);

// afterEach(() => {
// });
